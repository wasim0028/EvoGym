apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: evogym

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - migration-job.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: evogym
