apiVersion: v1
kind: ConfigMap
metadata:
  name: evogym-backend-config
data:
  NODE_ENV: "production"
  PORT: "5000"
  JWT_ACCESS_EXPIRES_IN: "15m"
  JWT_REFRESH_EXPIRES_IN: "7d"
  # CLIENT_URL is patched per environment in the overlays.
  CLIENT_URL: "http://localhost:5173"
