apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: evogym

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: evogym
